import randomImageName from "./src/js/randomImageNameGenerator";

document.addEventListener('DOMContentLoaded', () => {
    console.log('dom content loaded');
    console.log('random image name is ' + randomImageName);
 });