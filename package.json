{
  "name": "digifist-case",
  "version": "1.0.0",
  "description": "DigiFist case study for job interview",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "startCleanServer": "npm run buildAll && node -e \"setTimeout(() => process.exit(0), 2000)\" && node ./server/server.js 3005 && node -e \"setTimeout(() => process.exit(0), 2000)\" && open http://127.0.0.1:3005",
    "strt": "webpack",
    "js:cleanDist": "rm -rf public/js/*",
    "css:cleanDist": "rm -rf public/css/*",
    "all:cleanDist": "rm -rf dist/*",
    "js:build": "npm run js:cleanDist && npm run strt",
    "buildAll": "npm run all:cleanDist && npm run js:cleanDist && npm run css:cleanDist && npm run strt"
  },
  "author": "Emre Oztunc",
  "license": "ISC",
  "keywords": [],
  "devDependencies": {
    "@accessible360/accessible-slick": "^1.0.1",
    "autoprefixer": "^10.4.13",
    "bootstrap": "^5.2.3",
    "css-loader": "^6.7.3",
    "file-loader": "^6.2.0",
    "mini-css-extract-plugin": "^2.7.2",
    "node-sass": "^8.0.0",
    "postcss": "^8.4.21",
    "postcss-cli": "^10.1.0",
    "postcss-loader": "^7.0.2",
    "postcss-preset-env": "^7.8.3",
    "sass-loader": "^13.2.0",
    "webpack": "^5.75.0",
    "webpack-cli": "^5.0.1"
  }
}
